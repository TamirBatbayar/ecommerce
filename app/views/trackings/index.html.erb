<p id="notice"><%= notice %></p>

<h1>Listing Trackings</h1>

<table>
  <thead>
    <tr>
      <th>Session</th>
      <th>Product</th>
      <th>Page name</th>
      <th>Detail</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trackings.each do |tracking| %>
      <tr>
        <td><%= tracking.session_id %></td>
        <td><%= tracking.product_id %></td>
        <td><%= tracking.page_name %></td>
        <td><%= tracking.detail %></td>
        <td><%= link_to 'Show', tracking %></td>
        <td><%= link_to 'Edit', edit_tracking_path(tracking) %></td>
        <td><%= link_to 'Destroy', tracking, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tracking', new_tracking_path %>
